#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';

$tenant = $argv[1] ?? null;
if (!$tenant) {
    echo "Usage: tenant-create <tenant-id>\n";
    exit(1);
}

echo "Tenant {$tenant} created (stub).\n";
